{
    "name": "AWS EKS cluster version 2",
    "source": "CUSTOM",
    "description": "AWS cluster version should match specified (this is a simple rule for checking how custom rules work - in a real situation we prob want to specify a minimum version)",
    
    "provider": "AWS",
    "rule_text": "default allow = false allow { input.version == '1.20' }",
    "resource_type": "aws_eks_cluster",
    "severity": "Low"
   }